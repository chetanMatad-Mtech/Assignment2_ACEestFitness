apiVersion: v1
kind: Service
metadata:
  name: fitness-service
  namespace: fitness
spec:
  selector:
    app: fitness-app
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080

