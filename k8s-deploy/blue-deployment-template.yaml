apiVersion: apps/v1
kind: Deployment
metadata:
  name: DEPLOYMENT_NAME_PLACEHOLDER
  namespace: fitness
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fitness-app
      version: VERSION_PLACEHOLDER
      deployment: blue
  template:
    metadata:
      labels:
        app: fitness-app
        version: VERSION_PLACEHOLDER
        deployment: blue
    spec:
      containers:
      - name: fitness-app-container
        image: IMAGE_PLACEHOLDER
        ports:
        - containerPort: 9090
        env:
        - name: FLASK_ENV
          value: production

