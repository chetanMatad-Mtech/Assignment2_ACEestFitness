apiVersion: v1
kind: Service
metadata:
  name: fitness-service
  namespace: fitness
spec:
  type: LoadBalancer
  selector:
    version: green   # Switch to blue for rollback
  ports:
  - port: 80
    targetPort: 8080

