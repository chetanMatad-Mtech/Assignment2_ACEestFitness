apiVersion: v1
kind: Service
metadata:
  name: fitness-app-service
spec:
  selector:
    app: fitness-app
    version: VERSION_PLACEHOLDER  # updated dynamically in pipeline
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP

